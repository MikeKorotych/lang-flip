# lang-flip → Sayful

This app is now called **[Sayful](https://sayful.app)**.

- Download: https://github.com/MikeKorotych/sayful-releases/releases/latest
- This repository only hosts the legacy Sparkle update feed (`appcast.xml`)
  for installs older than 0.9.6.4, whose update URL is baked into the app.
